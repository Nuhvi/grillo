/*
 * FormAddList Messages
 *
 * This contains all the text for the FormAddList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormAddList';

export default defineMessages({
  submitButton: {
    id: `${scope}.submitButton`,
    defaultMessage: 'Add List',
  },
});
