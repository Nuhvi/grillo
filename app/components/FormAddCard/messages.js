/*
 * FormAddCard Messages
 *
 * This contains all the text for the FormAddCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormAddCard';

export default defineMessages({
  submitButton: {
    id: `${scope}.submitButton`,
    defaultMessage: 'Add Card',
  },
});
